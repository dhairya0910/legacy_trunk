<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Join a Family</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary-color: #007bff;
      --primary-hover: #0056b3;
      --background-color: #f4f7f9;
      --card-background: #ffffff;
      --text-color: #333;
      --border-color: #e0e0e0;
      --error-color: #d9534f;
      --error-background: #f2dede;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background-color: var(--background-color);
      color: var(--text-color);
      margin: 0;
      padding: 2rem;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .container {
      background: var(--card-background);
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
      padding: 2.5rem;
      max-width: 900px;
      width: 100%;
      box-sizing: border-box;
    }

    h1 {
      text-align: center;
      font-size: 2.2rem;
      font-weight: 600;
      margin-bottom: 2rem;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 1rem;
    }

    th, td {
      padding: 1rem;
      text-align: left;
      border-bottom: 1px solid var(--border-color);
    }

    thead {
      background-color: #f8f9fa;
    }

    th {
      font-weight: 600;
    }

    tbody tr:hover {
      background-color: #f1f3f5;
    }

    button {
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 8px;
      background-color: var(--primary-color);
      color: white;
      font-size: 0.9rem;
      font-weight: 500;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    button:hover:not([disabled]) {
      background-color: var(--primary-hover);
      transform: translateY(-2px);
    }

    button[disabled] {
      background-color: #cccccc;
      cursor: not-allowed;
      transform: none;
    }

    @media (max-width: 600px) {
      body {
        padding: 1rem;
      }
      .container {
        padding: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Join a Family</h1>

    <table>
      <thead>
        <tr>
          <th>Family Name</th>
          <th>Family ID</th>
          <th>Admin</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% if (families && families.length > 0) { %>
          <% families.forEach(family => { 
            const isMember = family.members.some(m => m.toString() === currentUserId.toString());
            const hasPendingRequest = family.joinRequests.some(r => r.userId.toString() === currentUserId.toString() && r.status === "pending");
          %>
            <tr>
              <td><%= family.family_name %></td>
              <td><%= family._id %></td>
              <td><%= family.admin?.name || family.admin?.username || 'No Admin' %></td>
              <td>
                <form action="/join-family/<%= family._id %>/send-request" method="POST">
                  <button type="submit" <% if (isMember || hasPendingRequest) { %> disabled <% } %>>
                    <% if (isMember) { %>
                      Already a Member
                    <% } else if (hasPendingRequest) { %>
                      Request Sent
                    <% } else { %>
                      Send Request
                    <% } %>
                  </button>
                </form>
              </td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="4" style="text-align: center; padding: 2rem;">No families found.</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
